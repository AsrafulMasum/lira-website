import ContainerLayout from "@/layout/ContainerLayout";
import React from "react";

const page = () => {
  return (
    <section className="bg-[#FAFFFC] min-h-[calc(100vh-64px)] pb-10">
      <ContainerLayout>page</ContainerLayout>
    </section>
  );
};

export default page;
